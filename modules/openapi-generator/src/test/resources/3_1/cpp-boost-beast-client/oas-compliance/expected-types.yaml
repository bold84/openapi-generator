# Expected C++ types for Gate A composed schemas.
# Every entry here MUST be a component schema whose ROOT-level defining keyword
# is oneOf, anyOf, or allOf (after skipping description/title/deprecated/example).
#
# This manifest is enforced by gate-a.sh: any root-composed schema whose
# emitted C++ type differs from the expectation causes a FAIL in the inventory.
# Conversely, any key in this file that does not correspond to a root-composed
# schema in fixtures.yaml is a stale entry and should be removed.
#
# Format:
#   schema_name: cpp_type
#
# Use std::variant<...> for oneOf over heterogeneous types,
# std::string / double / bool for anyOf primitive aliases,
# and fully qualified class names for object schemas.

# --- anyOf string + string-enum → CompositionBranchValue alias (validators preserved) ---
AnyOfStringStringEnum: std::variant<CompositionBranchValue<0, std::string>, CompositionBranchValue<1, std::string>>

# --- oneOf string + string-enum — Phase 3 preserves identity via CompositionBranchValue ---
OneOfStringStringEnum: std::variant<CompositionBranchValue<0, std::string>, CompositionBranchValue<1, std::string>>

# --- oneOf string | array<ItemStruct> ---
OneOfStringArray: std::variant<std::string, std::vector<ItemStruct>>

# --- allOf object merge (compatible) ---
AllOfObjectMerge: org::openapitools::client::model::AllOfObjectMerge

# --- anyOf [T, null] / nullable T → std::optional<T> ---
NullableString: std::optional<std::string>
NullableEnum: std::optional<std::string>

# --- discriminator + oneOf object branches ---
DiscriminatorOneOf: std::variant<Mammal, Bird>
Animal: std::variant<Mammal, Bird>

# --- multi-content stream event (oneOf 3 types) ---
MultiContentStreamEvent: std::variant<StreamProgress, StreamComplete, StreamError>

# --- multipart oneOf binary|object ---
MultipartMetaField: std::variant<std::vector<std::uint8_t>, MetaRecord>

# --- oneOf constrained numbers (multipleOf) — both branches type:number → CompositionBranchValue variant ---
ConstrainedNumber: std::variant<CompositionBranchValue<0, double>, CompositionBranchValue<1, double>>

# --- anyOf enum union [red] ∪ [blue] → CompositionBranchValue alias (validators preserved) ---
AnyOfEnumUnion: std::variant<CompositionBranchValue<0, std::string>, CompositionBranchValue<1, std::string>>

# --- all-null anyOf → CompositionBranchValue alias (null cardinality preserved) ---
AllNullAnyOf: std::variant<CompositionBranchValue<0, std::nullptr_t>, CompositionBranchValue<1, std::nullptr_t>>

# --- duplicate-null oneOf → CompositionBranchValue alias (null cardinality preserved) ---
DuplicateNullOneOf: std::variant<CompositionBranchValue<0, std::nullptr_t>, CompositionBranchValue<1, std::nullptr_t>>

# --- allOf enum intersection [a,b] ∩ [b,c] → std::string ---
AllOfEnumIntersection: std::string

# --- allOf conflicting optional property → object class ---
OptionalImpossibleAllOf: org::openapitools::client::model::OptionalImpossibleAllOf

# --- discriminator overlap — oneOf Mammal|Bird|OverlappingReptile ---
OverlappingAnimal: std::variant<Mammal, Bird, OverlappingReptile>

# --- oneOf [integer, number] → std::variant<int32_t, double> ---
IntOrNumber: std::variant<std::int32_t, double>
